# Blocky Bird — Flappy Bird com temática Minecraft

Siga as specs em `specs/` (requirements.md, design.md, tasks.md); use `uv` para tudo (`uv sync`, `uv run main.py`, `uv run pytest`); implemente uma task por vez, na ordem, e marque como `[x]` no `specs/tasks.md` ao concluir.
